[package]
name = "libtex"
version.workspace = true
edition.workspace = true

[lib]
name = "tex"
crate-type = ["cdylib", "staticlib", "rlib"]

[dependencies]
dlmalloc = { workspace = true }
tex-runtime = { path = "../tex-runtime" }
