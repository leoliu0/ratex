[package]
name = "tex-kpse"
version.workspace = true
edition.workspace = true

[dependencies]
zstd = { workspace = true }

[build-dependencies]
zstd = { workspace = true }
tar = { workspace = true }

[dev-dependencies]
tar = { workspace = true }

[features]
default = ["embed-packages"]
embed-packages = []
